# Full install from repository root: simulators + MaaSModelRefs + groups + Demo 01 entitlements.
# To use another demo, replace the second resource with the matching path (see deploy/README.md).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deploy/base
  - demos/01-one-to-one/manifests/maas-entitlements.yaml
